# permissions gate deny dogfood

Temporary pull request used to verify that the first `permissions` gate denies a pull request author who is not explicitly allowlisted.

This branch should be closed after the `Permissions Gate` workflow reports the expected denial.
